<?php
// Проверка, что цена была передана
if (isset($_POST['price'])) {
    // Получаем переданную цену
    $currentPrice = $_POST['price'];
    
    // Здесь вы можете использовать $currentPrice для дальнейших операций
    // Например, вывести или записать в базу данных
    echo "Полученная цена: " . htmlspecialchars($currentPrice);
} else {
    echo "Ошибка: цена не была передана.";
}
?>
